{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "PanoPose",
  "version": "1.2.0",
  "identifier": "dev.panopose.desktop",
  "build": {
    "beforeDevCommand": "sh -c 'if [ -f frontend/package.json ]; then npm run dev --prefix frontend; else npm run dev --prefix ../frontend; fi'",
    "beforeBuildCommand": "sh -c 'if [ -f frontend/package.json ]; then npm run build --prefix frontend; else npm run build --prefix ../frontend; fi'",
    "devUrl": "http://localhost:5173",
    "frontendDist": "../frontend/dist"
  },
  "app": {
    "windows": [
      {
        "title": "PanoPose",
        "width": 1440,
        "height": 960,
        "minWidth": 960,
        "minHeight": 640
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "externalBin": [
      "binaries/panopose-cli"
    ],
    "icon": [
      "icons/icon.png"
    ]
  }
}
